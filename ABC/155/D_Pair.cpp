#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < n; i++)

int main(void)
{
    int N, K;
    cin >> N >> K;

    vector<long long> a;
    rep(i, N) cin >> a[i];

    sort(a.begin(), a.end());

    
    return 0;
}